# Android Failures

## Known failure patterns

Background service killed by OS battery optimization mid-sync; companion app losing pairing session on OS-level app data clear; push notification delivery failure with no in-app fallback check.

## Cross-reference

See the corresponding subsystem file in `45-code-perfection-failure-modes/` for the code-level prevention checklist, and `docs/25-failure-modes/INDEX.md` for the full narrative failure-mode set.
