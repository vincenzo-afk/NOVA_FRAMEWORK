# Edition Comparison

At current scope NOVA ships a single, fully local-capable edition; this file exists as a placeholder contract for a future team/managed edition so any edition-specific feature work has a place to declare exactly what differs, rather than scattering `if (isPro)` checks ad hoc through the codebase.
