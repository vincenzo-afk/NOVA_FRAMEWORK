# AI Evaluation Benchmarks

Task-completion accuracy measured against a fixed labeled task set spanning each capability domain; run per model/provider change to catch quality regressions from a routing or prompt change.
