# Reasoning Tests

Multi-step planning correctness on tasks requiring 3+ sequential tool calls with dependencies between steps; failure here often indicates a Planner context-assembly bug, not a model-quality issue — cross-check against `43-ai-development/context-generation.md`.
