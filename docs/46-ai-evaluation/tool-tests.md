# Tool-Use Tests

Correct tool selection and correct argument construction, evaluated separately — a model can pick the right tool with wrong arguments, which is a distinct failure class needing its own metric.
