# Memory Usage (Resource)

Baseline idle RAM budget defined per platform (desktop vs. Android companion, which is far more constrained); any new observer or cache must be profiled against this budget.
