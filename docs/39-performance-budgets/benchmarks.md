# Benchmarks

Benchmark suite runs against a fixed reference dataset (synthetic memory corpus of defined size) so results are comparable release over release, not dependent on a developer's local data.
