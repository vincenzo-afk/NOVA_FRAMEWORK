# Dark Mode

Dark mode is not an inverted light theme — surface elevation is communicated via layered subtle color shifts, not just black backgrounds, to preserve depth cues. Follows OS setting by default with an explicit override in Settings.
